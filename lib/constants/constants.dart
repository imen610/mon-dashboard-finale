import 'package:flutter/material.dart';

// Colors used in this app

const primaryColor = Color(0xffFAAA1E);
const secondaryColor = Colors.white;
const bgColor = Color.fromRGBO(247, 251, 254, 1);
const textColor = Colors.black;
const lightTextColor = Colors.black26;
const transparent = Colors.transparent;

const grey = Color.fromRGBO(148, 170, 220, 1);
const purple = Color(0xffFF4C60);
const orange = Color.fromARGB(255, 60, 60, 228);
const green = Color.fromARGB(255, 64, 235, 170);
const red = Colors.red;

// Default App Padding
const appPadding = 16.0;

class appConstants {
  static const String KEY_ACCESS_TOKEN = "access_token";
  static const String USER_ID = 'user_id';
}
